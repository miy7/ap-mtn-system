import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
const errorResponse = (message: string, status: number, code: string) => NextResponse.json({ success: false, error: { code, message } }, { status, headers: { 'Cache-Control': 'no-store' } })
const text = (value: unknown, max = 160) => typeof value === 'string' ? value.trim().slice(0, max) : ''
const types = ['ELECTRICAL', 'CCTV']
export async function GET() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return errorResponse('กรุณาเข้าสู่ระบบ', 401, 'UNAUTHORIZED'); const { data, error } = await supabase.from('assets').select('id, asset_number, name, system_type, status, location, notes, customer_id, site_id, customers(name), customer_sites(name, address)').order('created_at', { ascending: false }); if (error) return errorResponse('ไม่สามารถโหลดข้อมูลอุปกรณ์ได้', 500, 'EQUIPMENT_LOAD_FAILED'); return NextResponse.json({ success: true, data }) }
export async function POST(request: Request) { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return errorResponse('กรุณาเข้าสู่ระบบ', 401, 'UNAUTHORIZED'); const body = await request.json().catch(() => null); const customerId = text(body?.customer_id, 50), siteId = text(body?.site_id, 50), assetNumber = text(body?.asset_number, 80), name = text(body?.name, 120), systemType = text(body?.system_type, 20); if (!customerId || !siteId || !assetNumber || !types.includes(systemType)) return errorResponse('ข้อมูลอุปกรณ์ไม่ครบถ้วน', 400, 'INVALID_EQUIPMENT'); const { data: site } = await supabase.from('customer_sites').select('id').eq('id', siteId).eq('customer_id', customerId).maybeSingle(); if (!site) return errorResponse('ไซต์ไม่สอดคล้องกับลูกค้า', 400, 'INVALID_SITE_SCOPE'); const { data, error } = await supabase.from('assets').insert({ customer_id: customerId, site_id: siteId, asset_number: assetNumber, name: name || null, system_type: systemType, location: text(body?.location, 160) || null, notes: text(body?.notes, 1000) || null }).select('id, asset_number, name, system_type, status, location, customer_id, site_id').single(); if (error) return errorResponse('ไม่สามารถสร้างอุปกรณ์ได้', 400, 'EQUIPMENT_CREATE_FAILED'); return NextResponse.json({ success: true, data }, { status: 201 }) }
export async function PATCH(request: Request) { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return errorResponse('กรุณาเข้าสู่ระบบ', 401, 'UNAUTHORIZED'); const body = await request.json().catch(() => null); const id = text(body?.id, 50), assetNumber = text(body?.asset_number, 80), name = text(body?.name, 120), systemType = text(body?.system_type, 20); if (!id || !assetNumber || !types.includes(systemType)) return errorResponse('ข้อมูลอุปกรณ์ไม่ครบถ้วน', 400, 'INVALID_EQUIPMENT'); const { data, error } = await supabase.from('assets').update({ asset_number: assetNumber, name: name || null, system_type: systemType, location: text(body?.location, 160) || null, notes: text(body?.notes, 1000) || null, updated_at: new Date().toISOString() }).eq('id', id).select('id, asset_number, name, system_type, status, location, notes').single(); if (error) return errorResponse('ไม่สามารถแก้ไขอุปกรณ์ได้', 400, 'EQUIPMENT_UPDATE_FAILED'); return NextResponse.json({ success: true, data }) }
